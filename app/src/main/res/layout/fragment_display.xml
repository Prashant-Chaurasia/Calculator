<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/bgcolor"
    android:paddingBottom="24dp"
    android:paddingTop="24dp"
    android:elevation="4dp"
    tools:context="com.example.prashant.calculator.DisplayFragment">

    <ImageButton
        style="@style/imgbuttonDisplay"
        android:id="@+id/imb_display_delete"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:src="@drawable/ic_clear_black_24dp"
        />
    
    <HorizontalScrollView
        style="@style/ScrollViewDisplay"
        android:id="@+id/hcv_display"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:layout_toEndOf="@+id/imb_display_delete"
        android:layout_toRightOf="@+id/imb_display_delete">
        
        <TextView
            style="@style/tvDisplay"
            android:id="@+id/tv_display"
            android:layout_width="wrap_content"
            android:layout_height="72dp"
            android:layout_gravity="bottom|end"
            tools:text="685-621"
            />
    </HorizontalScrollView>
</RelativeLayout>
